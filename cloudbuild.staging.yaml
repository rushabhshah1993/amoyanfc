# Google Cloud Build configuration for staging backend deployment
# This builds and deploys the Express backend to Cloud Run (Staging)

steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:$BUILD_ID'
      - '-t'
      - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:latest'
      - '--build-arg'
      - 'REACT_APP_API_URL=https://placeholder.run.app/graphql'
      - '--build-arg'
      - 'REACT_APP_BACKEND_URL=https://placeholder.run.app'
      - '--build-arg'
      - 'REACT_APP_COMPETITION_ID_IFC=67780dcc09a4c4b25127f8f6'
      - '--build-arg'
      - 'REACT_APP_COMPETITION_ID_IFL=67780e1d09a4c4b25127f8f8'
      - '--build-arg'
      - 'REACT_APP_COMPETITION_ID_CC=6778100309a4c4b25127f8fa'
      - '--build-arg'
      - 'REACT_APP_COMPETITION_ID_IC=6778103309a4c4b25127f8fc'
      - '-f'
      - 'Dockerfile'
      - '.'

  # Push the Docker image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:$BUILD_ID'

  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:latest'

  # Deploy to Cloud Run (Staging)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'amoyanfc-backend-staging'
      - '--image'
      - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:$BUILD_ID'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'NODE_ENV=staging'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '300s'
      - '--max-instances'
      - '3'
      - '--min-instances'
      - '0'

# Store the built images
images:
  - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:$BUILD_ID'
  - 'gcr.io/$PROJECT_ID/amoyanfc-backend-staging:latest'

# Timeout for the entire build
timeout: '1200s'

# Machine type for the build
options:
  machineType: 'N1_HIGHCPU_8'

